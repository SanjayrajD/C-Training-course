#include<iostream>
using namespace std;
int main()
{
    int n,x,y=1;
    cin>>n;
    int a[n];
    for(int i=0;i<n;i++)
    {
        cin>>a[i];
        x=x+a[i];
        y=y*a[i];
    }
    cout<<"Sum: "<<x<<endl;
    cout<<"product: "<<y;
}
